<Application x:Class="Table.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Table"
             StartupUri="NewMainWindow.xaml">
    <Application.Resources>
        <Style x:Key="DeskButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#556B80" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#586E83" />
                    <Setter Property="Cursor" Value="Hand" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
